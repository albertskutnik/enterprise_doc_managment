services:
  postgres:
    image: postgres:16
    container_name: edm-postgres
    enviroment:
      POSTGRES_DB: edm
      POSTGRES_USER: edm
      POSTGRES_PASSWORD: edm
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data


volumes:
  postgres-data:
